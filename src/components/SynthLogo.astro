---
interface Props {
  compact?: boolean;
  inverted?: boolean;
}

const { compact = false, inverted = false } = Astro.props;
---

<span class:list={["synth-logo", { compact, inverted }]} aria-label="Synth">
  <img src="/img/products/synth-logo-monochrome.svg" alt="Synth" />
</span>

<style>
  .synth-logo {
    display: inline-flex;
    align-items: center;
  }
  .synth-logo img {
    display: block;
    width: 150px;
    height: auto;
  }
  .synth-logo.compact img {
    width: 96px;
  }
  .synth-logo.inverted img {
    filter: brightness(0) invert(1);
  }
</style>
