---
import { Image } from "astro:assets";

type Logo = { src: string; alt: string; href?: string };

const logos: Logo[] = [
    { src: "/partners/target.png", alt: "Target" },
    { src: "/partners/greennet.png", alt: "GreenNet" },
    { src: "/partners/eu.png", alt: "European Commission" },
    { src: "/partners/telefonica.png", alt: "Telefónica" },
    {
        src: "/partners/tue.png",
        alt: "Eindhoven University of Technology (TU/e)",
    },
    { src: "/partners/tno.png", alt: "TNO" },
    { src: "/partners/parc.png", alt: "PARC" },
    { src: "/partners/vtt.png", alt: "VTT" },
];

// Duplicate once for a seamless loop
const loop = [...logos, ...logos];

type Stat = {
    value?: string;
    label: string;
    src?: string;
    alt?: string;
    href?: string;
};

const stats: Stat[] = [
    { value: "20+", label: "Partners" },
    { value: "4+", label: "Projects" },
    {
        src: "/partners/6g-ia-long.png",
        alt: "6G SNS member",
        href: "https://6gsns.eu/",
        label: "Official member of the 6G Smart Networks and Services Industry Association",
    },
    { value: "300+", label: "Active Deployments" },
    { value: "6+", label: "Open Source Products" },
];
---

<div id="partners" class="py-10">
    <div class="container-wide">
        <header class="mx-auto max-w-4xl text-center">
            <h2 class="mt-3 tracking-tight">
                Trusted by industry and research across Europe
            </h2>
        </header>

        <!-- Carousel / marquee -->
        <div
            class="logo-carousel mt-10"
            aria-label="Clients and partners logos"
        >
            <div class="logo-track">
                {
                    loop.map((l) => (
                        <div class="logo-item">
                            {l.href ? (
                                <a
                                    href={l.href}
                                    target="_blank"
                                    rel="noopener noreferrer"
                                    aria-label={l.alt}
                                >
                                    <Image
                                        src={l.src}
                                        alt={l.alt}
                                        width={320}
                                        height={120}
                                        loading="eager"
                                        decoding="async"
                                        class="dark:brightness-[2]"
                                    />
                                </a>
                            ) : (
                                <Image
                                    src={l.src}
                                    alt={l.alt}
                                    width={320}
                                    height={120}
                                    loading="eager"
                                    decoding="async"
                                    class="dark:brightness-[2]"
                                />
                            )}
                        </div>
                    ))
                }
            </div>
        </div>

        <ul
            class="mt-10 grid gap-4 sm:grid-cols-2 md:grid-cols-2 lg:grid-cols-6"
            aria-label="Key stats"
        >
            {
                stats.map((stat) =>
                    stat.src ? (
                        <li class="sm:col-span-2 md:col-span-2 lg:col-span-2">
                            <div class="flex h-full flex-col items-center justify-center gap-3 rounded-2xl border border-subtle bg-panel p-6 text-center shadow-sm">
                                {stat.href ? (
                                    <a
                                        href={stat.href}
                                        target="_blank"
                                        rel="noopener noreferrer"
                                        aria-label={stat.alt ?? stat.label}
                                        class="inline-flex items-center justify-center"
                                    >
                                        <Image
                                            src={stat.src}
                                            alt={stat.alt ?? ""}
                                            width={360}
                                            height={140}
                                            loading="eager"
                                            decoding="async"
                                            class="h-12 w-auto md:h-14 dark:brightness-[2]"
                                        />
                                    </a>
                                ) : (
                                    <Image
                                        src={stat.src}
                                        alt={stat.alt ?? ""}
                                        width={360}
                                        height={140}
                                        loading="eager"
                                        decoding="async"
                                        class="mx-auto h-12 w-auto md:h-14 dark:brightness-[2]"
                                    />
                                )}
                                <p class="text-sm font-medium text-muted">
                                    {stat.label}
                                </p>
                            </div>
                        </li>
                    ) : (
                        <li>
                            <div class="flex h-full flex-col items-center justify-center rounded-2xl border border-subtle bg-panel p-6 text-center shadow-sm">
                                <p class="text-3xl font-semibold tracking-tight text-primary">
                                    {stat.value}
                                </p>
                                <p class="mt-2 text-sm font-medium text-muted">
                                    {stat.label}
                                </p>
                            </div>
                        </li>
                    ),
                )
            }
        </ul>
        <!--
	        <p class="mt-8 text-center text-xs text-muted">
	            *Logos shown for recognition only. Ownership of trademarks belongs
	            to their respective holders.
	        </p> -->
    </div>
</div>

<style>
    .logo-carousel {
        position: relative;
        overflow: hidden;
        border-radius: 1rem;
        border: 1px solid rgba(255, 255, 255, 0.26);
        background: linear-gradient(
            180deg,
            rgba(255, 255, 255, 0.14),
            rgba(255, 255, 255, 0.06)
        );
        backdrop-filter: blur(16px) saturate(160%);
        -webkit-backdrop-filter: blur(16px) saturate(160%);
        box-shadow:
            inset 0 1px 0 rgba(255, 255, 255, 0.28),
            0 20px 48px rgba(0, 0, 0, 0.16);
        padding: 18px 14px;
    }

    :root[data-theme="dark"] .logo-carousel {
        border-color: rgba(255, 255, 255, 0.18);
        background: linear-gradient(
            180deg,
            rgba(255, 255, 255, 0.09),
            rgba(255, 255, 255, 0.03)
        );
        box-shadow:
            inset 0 1px 0 rgba(255, 255, 255, 0.12),
            0 24px 60px rgba(0, 0, 0, 0.35);
    }

    /* Soft edge fade like premium marquees */
    .logo-carousel::before,
    .logo-carousel::after {
        content: "";
        position: absolute;
        top: 0;
        width: 72px;
        height: 100%;
        z-index: 2;
        pointer-events: none;
    }
    .logo-carousel::before {
        left: 0;
        background: linear-gradient(
            to right,
            rgba(245, 247, 251, 0.9),
            transparent
        );
    }
    .logo-carousel::after {
        right: 0;
        background: linear-gradient(
            to left,
            rgba(245, 247, 251, 0.9),
            transparent
        );
    }

    :root[data-theme="dark"] .logo-carousel::before {
        background: linear-gradient(
            to right,
            rgba(7, 11, 18, 0.7),
            transparent
        );
    }

    :root[data-theme="dark"] .logo-carousel::after {
        background: linear-gradient(to left, rgba(7, 11, 18, 0.7), transparent);
    }

    .logo-track {
        display: flex;
        align-items: center;
        gap: 56px;
        width: max-content;
        animation: marquee 22s linear infinite;
        will-change: transform;
    }

    .logo-item {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        height: 42px;
    }

    .logo-item img {
        max-height: 28px;
        width: auto;
        max-width: 160px;
        opacity: 0.92;
        transition: opacity 160ms ease;
    }

    .logo-item a:hover img,
    .logo-item:hover img {
        opacity: 1;
    }

    /* Pause on hover (optional, feels nice) */
    .logo-carousel:hover .logo-track {
        animation-play-state: paused;
    }

    /* The trick: since we duplicated the list once,
     moving by 50% of the track width lands exactly at the second copy start. */
    @keyframes marquee {
        from {
            transform: translateX(0);
        }
        to {
            transform: translateX(-50%);
        }
    }

    @media (prefers-reduced-motion: reduce) {
        .logo-track {
            animation: none;
        }
        .logo-carousel {
            overflow-x: auto;
        }
    }

    @media (max-width: 640px) {
        .logo-track {
            gap: 36px;
            animation-duration: 18s;
        }
        .logo-item img {
            max-height: 24px;
            max-width: 140px;
        }
    }
</style>
